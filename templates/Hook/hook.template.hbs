import React from "react";

export default (arg: unknown) => {
  const [data, setData] = React.useState<unknown>();

  React.useEffect(() => {
    setData(arg);
  }, [arg]);

  return [data];
};