export default interface ImageProps {
    alt?: string;
    ariaLabel?: string,
    ariaLabelledBy?: string,
    classes?: string;
    external?: boolean;
    id?: string,
    role?: string;
    src: string;
};
