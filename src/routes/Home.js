import React from 'react';
import { Link } from 'react-router-dom';

const Home = () => (
    <main>
        <h2>Home</h2>
        <Link to="/contact">Contact</Link>
    </main>
);

export default Home;
